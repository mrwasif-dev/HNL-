# Public Assets

Static files served directly by the bot's Express server (`index.js`).

- `index.html` — the web dashboard used to view connection status, generate/scan the QR code, and manage the bot session.

Files placed in this folder are served at the root URL (e.g. `/index.html`).
